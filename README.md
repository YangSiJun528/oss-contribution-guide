# OSS 기여 시작 가이드

`OSS 기여 시작 가이드`의 mdBook 소스 저장소입니다.

## 로컬 미리보기

```sh
cargo install mdbook --version 0.5.4 --locked
mdbook serve --open
```

## 정적 사이트 빌드

```sh
mdbook build
```

빌드 결과는 `book/`에 생성됩니다.
